const quizzesController = require("./quizzes.controllers");
const questionsController = require("./questions.controller")
const submissionController=require("./submission.contoller");

module.exports = {
    quizzesController,
    questionsController,
    submissionController,
};
